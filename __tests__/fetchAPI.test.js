require('dotenv').config()
import fetchAPI from '../src/js/fetchAPI'

describe('New York city boroughs demographics', () => {
  test('dotEnv returns API key for the cityofnewyork.us website', () => {})
})
